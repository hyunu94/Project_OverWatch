<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tblBoard">
	
<select id="select_keyword" parameterType="hashMap" resultMap="hashMap">
	<![CDATA[
			 select * from tblBoard where keyword  like #{keyword} order by #{ref} desc, #{pos} asc limit #{start}, #{end};
	 ]]>
</select>

<select id="select_All" parameterType="hashMap" resultMap="hashMap">
	<![CDATA[
			 select * from tblBoard order by #{ref}desc, #{pos}asc limit  #{start}, #{end};
	 ]]>
</select>

<select id="select_countAll" parameterType="hashMap" resultMap="int">
	<![CDATA[
			select count(*) from tblBoard;
	 ]]>
</select>

<select id="select_countKey" parameterType="hashMap" resultMap="int">
	<![CDATA[
			select count(*) from tblBoard where #{keyField} like #{keyWord};
	 ]]>
</select>



</mapper>